data/*
!data/universe_contexts/
# Small, generated, and worth version-controlling: it maps Tinker run IDs to
# HF paths, facts, methods and LRs -- the provenance record for the sweep.
!data/tinker_models.json
# Hand-authored generation inputs: the domain taxonomy is the artifact that
# decides dataset breadth, so it is version-controlled and reviewed like code.
!data/umf_taxonomies/
!data/umf_taxonomies/*.json
.env
.venv
**/*results*/**
.vscode
**/__pycache__
__pycache__
science_synth_facts.egg-info
wandb
logs/
.cache
.cursorrules

# Large model files and data
*.pt
*.npz
*.bin
*.safetensors
AlphaEdit/data/
AlphaEdit/results/
*.json.gz
dump.rdb

# Probing outputs: keep the small result JSONs and plots, drop the tensors.
outputs/**/*.pt
